/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "media.dodostatic.net",
      "cdn.dodostatic.net",
      "media.dodostatic.com",
    ],
  },
};

export default nextConfig;
